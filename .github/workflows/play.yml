name: Build Linux Kernel in VM

# This workflow runs on a self-hosted runner with the "linux" label
on:
  push:
    branches: [ main ]
#  schedule:
 #   - cron: '0 0 * * *'  # Run every day at midnight (adjust as needed)

jobs:
  build:
    runs-on: local  # Specify self-hosted runner with "linux" label
    steps:
      - uses: actions/checkout@v3  # Checkout the repository code

      - name: Bring up VM
        run: |
          vagrant up
          vagrant ssh -c "pwd && ls -al"
      
      - name: Update git repo
        run: |
          vagrant ssh -c "echo $PWD"
          #vagrant ssh -c "cd linux" # && git pull"

      - name: Build and install kernel, reboot
        id: build
        run: |
          #vagrant ssh -c "cd linux && make clean -j$(nproc)"
          #vagrant ssh -c "cd linux && make defconfig"
          #vagrant ssh -c "cd linux && make bzImage -j$(nproc)"
          #vagrant ssh -c "cd linux && make modules -j$(nproc)"
          #vagrant ssh -c "cd linux && sudo make modules_install -j$(nproc)"
          #vagrant ssh -c "cd linux && sudo make install -j$(nproc)"
          vagrant ssh -c "gcc hello.c"
          if: build.outputs.num == 0
            run: echo zero
          
          vagrant ssh -c "./a.out"
                    
          vagrant halt

      - name: Bring up VM after reboot
        run: |
           vagrant up
           vagrant ssh -c "uname -r"

